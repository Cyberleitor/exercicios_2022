#!/usr/bin/env node

const ferrari = {
	modelo: "F40",
	maxSpeed: 324
}

const volvo = {
	modelo: "V40",
	maxSpeed: 200
}

console.log(ferrari.__proto__)
console.log(ferrari.__proto__ === Object.prototype)
console.log(ferrari.__proto__ === Object.prototype)
